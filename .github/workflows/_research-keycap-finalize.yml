name: Finalize frozen Keycap baseline

on:
  push:
    branches:
      - research/keycap-finalize-2026-09-14

permissions:
  contents: read
  actions: read

jobs:
  finalize:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v7
        with:
          python-version: '3.12'
          cache: pip
          cache-dependency-path: |
            pyproject.toml
            constraints-dev.txt
      - name: Install Reaper
        run: |
          python -m pip install "pip==26.2.1"
          pip install -c constraints-dev.txt -e .
      - name: Download frozen run 2 evidence
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          mkdir -p baseline
          gh run download 34831092303 \
            --repo "${{ github.repository }}" \
            --name keycap-traction-2026-09-14 \
            --dir baseline
          test -f baseline/keycap-traction-result.json
          test -d baseline/artifacts
          cp -R baseline/artifacts ./artifacts
      - name: Build hash-bound review and observe known direct listing
        run: python research/keycap-traction-2026-09-14/finalize_evidence.py
      - name: Build reviewed intelligence from frozen baseline
        shell: bash
        run: |
          set -euo pipefail
          current_artifact="$(find artifacts/exports/keycap-traction-2026-09-14 -type f -name '*.zip' | sort | tail -n 1)"
          test -n "$current_artifact"
          yandex-reaper-thesis build \
            research/keycap-traction-2026-09-14/suite.json \
            "$current_artifact" \
            --review research/keycap-traction-2026-09-14/directness-review.json \
            | tee keycap-reviewed-result.json
      - name: Upload final decision evidence
        uses: actions/upload-artifact@v4
        with:
          name: keycap-traction-final-2026-09-14
          path: |
            baseline/keycap-traction-result.json
            keycap-reviewed-result.json
            known-direct-553722.json
            research/keycap-traction-2026-09-14/suite.json
            research/keycap-traction-2026-09-14/directness-review.json
            artifacts/
          if-no-files-found: error
          retention-days: 30
